[project]
name = "dbt"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Nicolas2320", email = "nicolasvalderramabarrera@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "dbt-postgres==1.9.0",
]

[project.scripts]
dbt = "dbt:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
